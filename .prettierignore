# docs/tldr.md is a tldr-pages draft and has to stay byte-for-byte in that
# project's style; everything else prettier could format is either generated
# (dist/) or not markdown.
docs/tldr.md
dist/
# Untracked by design: a host's own agent notes, and agent worktrees (whole
# second checkouts, formatted where they came from).
CLAUDE.local.md
.claude/worktrees/
