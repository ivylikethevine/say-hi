# Paths prettier must never format: the tldr-pages draft (kept byte-for-byte
# in that project's style), build output, and untracked agent notes and
# worktrees.
docs/tldr.md
dist/
CLAUDE.local.md
.claude/worktrees/
